---
title: "Active Directory Series: Part 5 - Persistence"
description: "How an attacker stays in a domain after the initial compromise is patched: Golden, Silver, and Diamond Tickets, the Skeleton Key, DSRM as a hidden local backdoor on a domain controller, malicious custom SSPs that log every credential in plaintext, and ACL-based persistence through AdminSDHolder, each with the specific event or artifact that gives it away."
date: "2026-05-24"
author: "Mohamed Habib Jaouadi"
tags: ["active-directory", "windows", "red-team", "blue-team", "persistence", "kerberos", "detection-engineering", "active-directory-series"]
banner: "/banners/posts/active-directory-persistence.jpg"
bannerAlt: "A tree growing out of a concrete wall, evoking persistence mechanisms that keep working even after the surface is repaired"
visibility: "public"
series: "Active Directory Series"
seriesOrder: 5
---

> **Series Focus:** Parts 3 and 4 covered getting in and moving around. This part covers staying in after the specific hole that let an attacker in gets patched, which is a different problem: persistence in Active Directory usually doesn't mean malware on a box, it means a forged credential, a patched authentication path, or a permission nobody remembered to look for.

**TL;DR:** A <Term concept="golden-ticket">Golden Ticket</Term> forged from the krbtgt hash survives until that hash is rotated twice, covered fully in Part 3. A <Term concept="silver-ticket">Silver Ticket</Term> is quieter but narrower: forged from one service account's hash, it never touches a domain controller at all. A <Term concept="diamond-ticket">Diamond Ticket</Term> starts from a real ticket and edits it, evading the detections built for tickets forged entirely from scratch. A <Term concept="sapphire-ticket">Sapphire Ticket</Term> goes one step further: instead of editing a PAC, it transplants an already-legitimate one lifted from a privileged account via S4U2Self and U2U, evading even PAC-versus-directory correlation checks, at least until a fully patched domain controller notices the transplanted PAC is missing structures only a real TGT should have. The <Term concept="skeleton-key">Skeleton Key</Term> patches a domain controller's LSASS in memory so every account authenticates with its real password or one attacker-chosen master password, until the DC reboots. <Term concept="dsrm">DSRM</Term> abuse turns a rarely-rotated local account on every DC into a backdoor that survives a full domain password reset. A <Term concept="custom-ssp">custom SSP</Term> sits inside LSASS logging every credential that authenticates through it, in plaintext, indefinitely. And ACL-based persistence through <Term concept="adminsdholder">AdminSDHolder</Term> means a permission an admin already removed can come back on its own, every hour, until the actual backdoor is found.

## Introduction

Getting Domain Admin once is, per Part 3, mostly a matter of finding the right misconfigured ACE, delegation setting, or certificate template. Staying Domain Admin after that specific issue gets fixed is a different skill, and it's the one most defenders think about last. Every technique in this post assumes the attacker already reached high privilege at some point and is now answering a narrower question: what survives a password reset, a patch, or an incident responder's first pass at cleanup?

Same rules as the rest of this series: every technique explained from what it actually does, a diagram wherever the mechanism isn't obvious from a sentence, and a real detection signal for each one rather than a vague "monitor for anomalies."

## Golden, Silver, Diamond, and Sapphire Tickets: Four Ways to Forge Kerberos

The <Term concept="golden-ticket">Golden Ticket</Term> is Part 3's endgame and won't be re-explained here: forge a TGT entirely offline from the krbtgt hash, valid for any username and any group membership, until krbtgt is rotated twice. What's worth adding here is the other two members of the family, because they trade the Golden Ticket's domain-wide reach for properties that matter specifically for staying hidden.

A <Term concept="silver-ticket">Silver Ticket</Term> skips the TGT step entirely. Instead of krbtgt's hash, it uses the password hash of a single service account, a SQL Server service account, a specific computer account, whatever the attacker's target actually is, and forges a TGS (service ticket) directly for that service. The forged TGS is presented straight to the target service, which decrypts it with its own key and, in most default configurations, never checks back with a domain controller to confirm the ticket is legitimate. That absence of a check is the entire point: a Golden Ticket still has to go through a KDC to get a service ticket after the TGT is minted, generating an ordinary **event 4769**, but a Silver Ticket generates neither a 4768 nor a 4769 anywhere, because the KDC was never involved at all. The cost is scope: it only works against the one service tied to the stolen hash, not the whole domain.

A <Term concept="diamond-ticket">Diamond Ticket</Term> solves a different problem: a forged-from-scratch Golden Ticket carries small tells, Mimikatz's default ten-year ticket lifetime being the most cited one, that detection tooling specifically looks for. A Diamond Ticket starts from a real TGT, requested normally through an actual AS-REQ/AS-REP exchange with a real domain controller, decrypts it offline using the krbtgt hash, edits the fields that matter (group memberships, most often), and re-encrypts it with the same krbtgt hash. Since the ticket really was issued by a DC, its lifetime, policy values, and PAC structure all look exactly like they should, because they are.

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 210" style={{ width: "100%", maxWidth: "600px" }} role="img" aria-label="Two rows contrasting Golden Ticket and Silver Ticket. In the Golden Ticket row, an attacker forges a TGT offline with the krbtgt hash and still must present it to the KDC to get a service ticket, generating an event 4769. In the Silver Ticket row, an attacker forges a TGS directly with a stolen service account hash and presents it straight to the target service, with the KDC never contacted at all.">
    <text x="15" y="22" fill="currentColor" fontSize="10.5" fontWeight="bold" opacity="0.85">Golden Ticket: forged TGT, still touches the KDC once</text>
    <rect x="10" y="35" width="120" height="44" rx="7" fill="#ef4444" fillOpacity="0.1" stroke="#ef4444" strokeWidth="1.2" />
    <text x="70" y="61" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Attacker</text>
    <rect x="480" y="35" width="140" height="44" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="550" y="61" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">KDC</text>
    <line x1="130" y1="57" x2="480" y2="57" stroke="#ef4444" strokeOpacity="0.65" strokeWidth="1.2" markerEnd="url(#adTixA)" />
    <text x="305" y="47" textAnchor="middle" fill="#ef4444" fontSize="8" opacity="0.85">TGS-REQ using the forged TGT: logged as event 4769</text>

    <text x="15" y="112" fill="currentColor" fontSize="10.5" fontWeight="bold" opacity="0.85">Silver Ticket: forged TGS from a service hash, not krbtgt</text>
    <rect x="10" y="125" width="120" height="44" rx="7" fill="#ef4444" fillOpacity="0.1" stroke="#ef4444" strokeWidth="1.2" />
    <text x="70" y="151" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Attacker</text>
    <rect x="480" y="125" width="140" height="44" rx="7" fill="currentColor" fillOpacity="0.06" stroke="currentColor" strokeWidth="1" strokeOpacity="0.35" />
    <text x="550" y="147" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Target service</text>
    <text x="550" y="160" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.7">(SQL, file share, ...)</text>
    <line x1="130" y1="147" x2="480" y2="147" stroke="#10b981" strokeOpacity="0.7" strokeWidth="1.2" markerEnd="url(#adTixB)" />
    <text x="305" y="137" textAnchor="middle" fill="#10b981" fontSize="8" opacity="0.9">Forged TGS presented directly: no 4768, no 4769, anywhere</text>
    <defs>
      <marker id="adTixA" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#ef4444" opacity="0.7" />
      </marker>
      <marker id="adTixB" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#10b981" opacity="0.8" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>Broader reach costs KDC visibility; narrower reach buys silence. A Silver Ticket's stealth is scoped to exactly the one service it was forged for.</p>
</div>

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 150" style={{ width: "100%", maxWidth: "600px" }} role="img" aria-label="A four-step horizontal chain for a Diamond Ticket: request a real TGT through a normal AS-REQ and AS-REP exchange, decrypt it offline with the krbtgt hash, modify fields such as group membership, and re-encrypt it with the same krbtgt hash, producing a ticket that looks exactly like a legitimately issued one." >
    <rect x="10" y="60" width="140" height="50" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="80" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Request a real TGT</text>
    <text x="80" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">(normal AS-REQ)</text>
    <rect x="180" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="250" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Decrypt offline</text>
    <text x="250" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">with krbtgt hash</text>
    <rect x="350" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="420" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Modify fields</text>
    <text x="420" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">(group membership)</text>
    <rect x="520" y="60" width="110" height="50" rx="7" fill="#ef4444" fillOpacity="0.12" stroke="#ef4444" strokeWidth="1.3" />
    <text x="575" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Re-encrypt:</text>
    <text x="575" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">looks legitimate</text>
    <line x1="150" y1="85" x2="180" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adDiamond)" />
    <line x1="320" y1="85" x2="350" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adDiamond)" />
    <line x1="490" y1="85" x2="520" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adDiamond)" />
    <defs>
      <marker id="adDiamond" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>The first step is the only one that touches a domain controller, and it's a completely ordinary authentication, indistinguishable from any other logon.</p>
</div>

The `/ldap` and `/opsec` flags on the newer `diamond` implementation are specifically built to match real Windows AS-REQ/AS-REP behavior and pull realistic PAC values, closing the gap that made earlier diamond tickets detectable too.

Diamond Tickets still have one tell left: editing a PAC's group memberships by hand can drift from what the directory actually says about that account, and a detector correlating ticket contents against AD catches exactly that drift. A <Term concept="sapphire-ticket">Sapphire Ticket</Term> removes the hand-editing step entirely. Instead of writing a privileged group SID into a PAC, it borrows one that's already legitimate: the attacker runs an S4U2Self request as a service the attacker controls, immediately followed by a U2U (User-to-User) exchange, targeting a genuinely privileged account, which yields a real service ticket carrying that privileged account's real PAC. That PAC, exactly as issued by the KDC, is then spliced into a low-privileged user's own real TGT and re-encrypted with the krbtgt hash. Nothing in the resulting ticket was hand-edited, so there's no drift between the PAC and the directory to correlate against, only a PAC that belongs to a different account than the ticket claims to.

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 260" style={{ width: "100%", maxWidth: "620px" }} role="img" aria-label="A four-step vertical chain for a Sapphire Ticket. Step one, the attacker runs an S4U2Self request as a service it controls, targeting a genuinely privileged account. Step two, a U2U exchange against that same request yields a real service ticket carrying the privileged account's real PAC. Step three, the attacker extracts that PAC and splices it into a low-privileged user's own real TGT. Step four, the spliced ticket is re-encrypted with the krbtgt hash, producing a ticket whose PAC was never hand-edited, only transplanted from a different, genuinely issued ticket.">
    <rect x="70" y="10" width="500" height="38" rx="6" fill="#0ea5e9" fillOpacity="0.1" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="320" y="34" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">1. S4U2Self as a controlled service, impersonating a genuinely privileged account</text>
    <rect x="70" y="64" width="500" height="38" rx="6" fill="#0ea5e9" fillOpacity="0.1" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="320" y="88" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">2. U2U exchange: yields a real service ticket carrying that account's real PAC</text>
    <rect x="70" y="118" width="500" height="38" rx="6" fill="#a855f7" fillOpacity="0.1" stroke="#a855f7" strokeWidth="1.2" />
    <text x="320" y="142" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">3. Extract that PAC, splice it into a low-privileged user's own real TGT</text>
    <rect x="70" y="172" width="500" height="38" rx="6" fill="#ef4444" fillOpacity="0.12" stroke="#ef4444" strokeWidth="1.3" />
    <text x="320" y="196" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">4. Re-encrypt with the krbtgt hash: no field was ever hand-edited</text>
    <rect x="120" y="226" width="400" height="28" rx="6" fill="currentColor" fillOpacity="0.05" stroke="currentColor" strokeWidth="1" strokeOpacity="0.3" strokeDasharray="4 3" />
    <text x="320" y="245" textAnchor="middle" fill="currentColor" fontSize="8.5" fontWeight="bold">missing: the Requestor / Attributes PAC structures a real TGT carries</text>
    <line x1="320" y1="48" x2="320" y2="64" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSapphire)" />
    <line x1="320" y1="102" x2="320" y2="118" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSapphire)" />
    <line x1="320" y1="156" x2="320" y2="172" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSapphire)" />
    <line x1="320" y1="210" x2="320" y2="226" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSapphire)" />
    <defs>
      <marker id="adSapphire" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>The PAC itself is never hand-edited, which is exactly what defeats Diamond Ticket detection. But it's a service-ticket PAC wearing a TGT's clothes, and that mismatch is its own tell on a patched DC.</p>
</div>

**Requirements:** a Silver Ticket needs only the target service account's password hash, not krbtgt, and never needs Domain Admin if that one hash is already in hand. A Diamond Ticket needs krbtgt's hash plus a valid account to request the initial real TGT from, the same prerequisite as a Golden Ticket. A Sapphire Ticket needs krbtgt's hash or AES key, control of an account with an SPN to run the S4U2Self request from, and knowledge of a genuinely privileged account to target for its PAC.

```bash
# Silver Ticket: forge a TGS directly from a service account's hash, no KDC involved
Rubeus.exe silver /service:cifs/fileserver.$DOMAIN /rc4:$SERVICE_HASH /user:anyuser /domain:$DOMAIN /sid:$DOMAIN_SID /ptt

# Diamond Ticket: starts from a real TGT, edits it, re-encrypts with krbtgt's hash
Rubeus.exe diamond /tgtdeleg /ticketuser:anyuser /ticketuserid:1234 /groups:512 /krbkey:$KRBTGT_HASH /domain:$DOMAIN /dc:$TARGET_DC /opsec /ptt

# Sapphire Ticket: Impacket's ticketer.py, transplanting a privileged account's real PAC
ticketer.py -request -impersonate administrator -domain-sid $DOMAIN_SID -domain $DOMAIN -aesKey $KRBTGT_AES_KEY -user-id 1104 lowpriv-user
```

**Detection.** Silver Tickets are the hardest of the three older variants by default, precisely because no KDC event exists to alert on; the one server-side control that catches them is setting `ValidateKdcPacSignature` to require domain controllers to validate the PAC signature on presented tickets, which a forged Silver Ticket won't have. Diamond Tickets are caught the same way Golden Tickets sometimes aren't: since the initial TGT request is completely ordinary, detection has to focus on what happens after, unexpected group membership appearing in a ticket's PAC relative to what the account actually holds in AD, which requires correlating ticket contents against the directory rather than looking for anomalies in the ticket request itself. Sapphire Tickets are built specifically to survive that correlation check, since the PAC really does belong to the privileged account it claims, which is exactly why Microsoft's October 2022 patch (KB5008380, issued alongside the fix for CVE-2021-42287) added two PAC structures, Requestor and Attributes, that only ever appear in a genuinely issued TGT: a transplanted service-ticket PAC doesn't have them, and a fully patched domain controller rejects the ticket with `KDC_ERR_TGT_REVOKED` on presentation. On a domain that hasn't applied that patch, a Sapphire Ticket has no reliable detection at all.

## The Skeleton Key: One Password, Every Account

The <Term concept="skeleton-key">Skeleton Key</Term> takes a completely different approach: instead of forging tickets, it patches the authentication code path directly, in memory, on a domain controller. With Domain Admin rights and `SeDebugPrivilege`, Mimikatz's `misc::skeleton` injects into `lsass.exe` on the target DC, downgrades Kerberos encryption to RC4 for that authentication path, and installs a single master password that works alongside every account's real password, for every account in the domain, without changing anyone's actual password.

**Requirements:** Domain Admin (or equivalent) and `SeDebugPrivilege` on the target domain controller, since injecting into that DC's `lsass.exe` is the entire mechanism.

```bash
mimikatz.exe "privilege::debug" "misc::skeleton" "exit"

# from that point, any account authenticates domain-wide with its real password or:
net use \\fileserver\share /user:anyuser Mimikatz
```

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 150" style={{ width: "100%", maxWidth: "600px" }} role="img" aria-label="A four-step horizontal chain for the Skeleton Key attack: Domain Admin rights and SeDebugPrivilege are used to patch lsass.exe in memory on a domain controller, Kerberos encryption is downgraded to RC4 for that authentication path, a master password is installed alongside every account's real password, and any account can then authenticate domain-wide with its real password or the master password until the domain controller reboots." >
    <rect x="10" y="60" width="140" height="50" rx="7" fill="#f59e0b" fillOpacity="0.12" stroke="#f59e0b" strokeWidth="1.2" />
    <text x="80" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Patch lsass.exe</text>
    <text x="80" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">on a DC in memory</text>
    <rect x="180" y="60" width="140" height="50" rx="7" fill="#f59e0b" fillOpacity="0.12" stroke="#f59e0b" strokeWidth="1.2" />
    <text x="250" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Downgrade to RC4</text>
    <text x="250" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">for that auth path</text>
    <rect x="350" y="60" width="140" height="50" rx="7" fill="#f59e0b" fillOpacity="0.12" stroke="#f59e0b" strokeWidth="1.2" />
    <text x="420" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Install a master</text>
    <text x="420" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">password, domain-wide</text>
    <rect x="520" y="60" width="110" height="50" rx="7" fill="#ef4444" fillOpacity="0.12" stroke="#ef4444" strokeWidth="1.3" />
    <text x="575" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Real or master</text>
    <text x="575" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">password both work</text>
    <line x1="150" y1="85" x2="180" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSkel)" />
    <line x1="320" y1="85" x2="350" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSkel)" />
    <line x1="490" y1="85" x2="520" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSkel)" />
    <defs>
      <marker id="adSkel" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>The one built-in limit: it's an in-memory patch, not a persistent change, so a DC reboot clears it. Real deployments re-inject it after every restart if they want it to last.</p>
</div>

**Detection.** Because it's an in-memory LSASS patch rather than a registry or file-based change, detection means catching the patch itself: configuring `AuditLevel` under `Image File Execution Options\lsass.exe` to `00000008` enables auditing for code injection and unsigned modules loaded into LSASS, and any authentication that succeeds with a password that isn't the account's actual current one, however that gets caught operationally, is close to a smoking gun.

## DSRM: The Backdoor Nobody Thinks of as an AD Object

Every domain controller has a <Term concept="dsrm">DSRM</Term> (Directory Services Restore Mode) local Administrator account, with a password set once at DC promotion and, in most environments, never rotated again. Normally that account only matters when the DC is booted into DSRM for database repair. The registry value `HKLM\System\CurrentControlSet\Control\Lsa\DsrmAdminLogonBehavior` controls when it can be used more broadly: `0` (default) restricts it to DSRM boot, `1` allows it if the AD DS service is stopped, and `2`, which doesn't exist by default and has to be created, allows it to authenticate over the network at any time, like an ordinary local administrator on that DC.

**Requirements:** Domain Admin (or local admin on the DC itself) to read or reset the DSRM password and to set the registry value in the first place.

```bash
# extract the DSRM local admin hash from the DC's own SAM
secretsdump.py -just-dc-user Administrator $DOMAIN/$USER:$PASSWORD@$TARGET_DC

# set the registry value that allows network logon
reg.exe add "HKLM\System\CurrentControlSet\Control\Lsa" /v DsrmAdminLogonBehavior /t REG_DWORD /d 2

# authenticate as the DSRM account, over the network, going forward
netexec smb $TARGET_DC -u Administrator -H $DSRM_HASH --local-auth
```

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 150" style={{ width: "100%", maxWidth: "600px" }} role="img" aria-label="A four-step horizontal chain for DSRM persistence: a domain controller's DSRM local administrator password, set once at promotion and rarely rotated, is extracted or reset by an attacker, the DsrmAdminLogonBehavior registry value is set to 2, and the DSRM account can then authenticate over the network like an ordinary local admin, surviving a full domain password reset because it is a local account rather than an Active Directory object." >
    <rect x="10" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="80" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">DSRM password set</text>
    <text x="80" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">once, rarely rotated</text>
    <rect x="180" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="250" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Attacker extracts</text>
    <text x="250" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">or resets it</text>
    <rect x="350" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="420" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Sets DsrmAdmin-</text>
    <text x="420" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">LogonBehavior = 2</text>
    <rect x="520" y="60" width="110" height="50" rx="7" fill="#ef4444" fillOpacity="0.12" stroke="#ef4444" strokeWidth="1.3" />
    <text x="575" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Network logon,</text>
    <text x="575" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">not an AD object</text>
    <line x1="150" y1="85" x2="180" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adDsrm)" />
    <line x1="320" y1="85" x2="350" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adDsrm)" />
    <line x1="490" y1="85" x2="520" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adDsrm)" />
    <defs>
      <marker id="adDsrm" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>Every domain user and computer password can be reset and this backdoor is untouched, because it was never a domain account to begin with.</p>
</div>

**Detection.** The registry write itself is the reliable signal, `DsrmAdminLogonBehavior` being set to `1` or especially `2` has no legitimate reason to happen outside a documented recovery procedure, and it's a straightforward Sysmon or native registry-auditing detection to build. The resulting logons show up as ordinary **event 4624** for the local DSRM account name, unremarkable unless someone is specifically watching for that account authenticating at all.

## Custom SSP: A Standing Wiretap Inside LSASS

Windows loads Security Support Provider DLLs into LSASS at boot to handle each supported authentication protocol, Kerberos, NTLM, Negotiate, listed in the `Security Packages` value under `HKLM\SYSTEM\CurrentControlSet\Control\Lsa`. A <Term concept="custom-ssp">custom SSP</Term> adds one more DLL to that list, and because every credential that authenticates on the box passes through every loaded SSP, a malicious one sees all of it, in plaintext, indefinitely.

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 150" style={{ width: "100%", maxWidth: "600px" }} role="img" aria-label="A four-step horizontal chain for a custom SSP: a malicious DLL is placed on disk and registered in the Security Packages registry value, LSASS loads it at the next boot, or it is injected directly into a running LSASS without a reboot, and from then on every credential authenticating through that machine is logged in plaintext." >
    <rect x="10" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="80" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">DLL registered in</text>
    <text x="80" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Security Packages</text>
    <rect x="180" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="250" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">LSASS loads it at</text>
    <text x="250" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">boot (or injected live)</text>
    <rect x="350" y="60" width="140" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="420" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Sees every credential</text>
    <text x="420" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">that authenticates</text>
    <rect x="520" y="60" width="110" height="50" rx="7" fill="#ef4444" fillOpacity="0.12" stroke="#ef4444" strokeWidth="1.3" />
    <text x="575" y="82" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">Logged in</text>
    <text x="575" y="95" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">plaintext, forever</text>
    <line x1="150" y1="85" x2="180" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSsp)" />
    <line x1="320" y1="85" x2="350" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSsp)" />
    <line x1="490" y1="85" x2="520" y2="85" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSsp)" />
    <defs>
      <marker id="adSsp" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>Registered on a domain controller, this quietly captures the plaintext password of every user who authenticates against that DC from that point on.</p>
</div>

Two variants exist, and they trade persistence for stealth in opposite directions from each other: dropping Mimikatz's `mimilib.dll` and adding it to the registry survives a reboot but leaves a file and a registry change behind; Mimikatz's `misc::memssp` patches a running LSASS directly with no reboot and no file dropped, but the injection doesn't survive one either.

**Requirements:** Domain Admin (or local admin) on the target host, ideally a domain controller, to write `mimilib.dll` and its registry entry, or `SeDebugPrivilege` for the in-memory-only variant.

```bash
# persistent, survives reboot: drop the DLL and register it, then reboot or restart LSA
copy mimilib.dll C:\Windows\System32\
reg.exe add "HKLM\SYSTEM\CurrentControlSet\Control\Lsa" /v "Security Packages" /t REG_MULTI_SZ /d "kerberos\0msv1_0\0mimilib" /f

# in-memory only, no reboot needed, doesn't survive one either
mimikatz.exe "privilege::debug" "misc::memssp" "exit"

# captured credentials land in, by default:
type C:\Windows\System32\kiwissp.log
```

**Detection:** monitor the `Security Packages` registry value for unexpected additions (**event 4657** for the change itself), and treat any unsigned or unexpected DLL loaded into `lsass.exe`, visible via Sysmon **event 7** (ImageLoad), as worth investigating on sight.

## ACL-Based Persistence: AdminSDHolder and the Permission That Comes Back

Every technique so far forges or patches something. This one just grants a permission and waits, and it's arguably the hardest to fully clean up because removing it from where it's visible doesn't actually remove it.

<Term concept="adminsdholder">AdminSDHolder</Term> is a special AD object whose ACL gets copied onto every member of a set of protected groups, Domain Admins, Enterprise Admins, and similar, by a process called SDProp, which runs on the PDC Emulator (the same FSMO role from Part 1) every 60 minutes by default. It exists to stop an admin from accidentally weakening permissions on a Domain Admin account by editing it directly instead of through the group. An attacker who adds their own ACE, say `GenericAll`, to AdminSDHolder itself gets that ACE copied onto every protected account on the very next SDProp cycle, and it keeps coming back on every cycle after that, regardless of what anyone does to the protected accounts directly.

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 220" style={{ width: "100%", maxWidth: "560px" }} role="img" aria-label="A cyclical diagram: an attacker adds an ACE to AdminSDHolder, the SDProp process, running on the PDC Emulator every 60 minutes, copies that ACE onto Domain Admins and every other protected group, an administrator removes the ACE directly from Domain Admins, and the cycle loops back because removing it there has no lasting effect, only removing it from AdminSDHolder itself stops the reapplication.">
    <rect x="240" y="10" width="160" height="46" rx="8" fill="#ef4444" fillOpacity="0.12" stroke="#ef4444" strokeWidth="1.3" />
    <text x="320" y="30" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Attacker adds ACE to</text>
    <text x="320" y="43" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">AdminSDHolder</text>
    <rect x="240" y="90" width="160" height="52" rx="8" fill="#f59e0b" fillOpacity="0.12" stroke="#f59e0b" strokeWidth="1.2" />
    <text x="320" y="108" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">SDProp (PDC Emulator,</text>
    <text x="320" y="121" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">every ~60 min) copies</text>
    <text x="320" y="134" textAnchor="middle" fill="currentColor" fontSize="9" fontWeight="bold">it onto Domain Admins</text>
    <rect x="240" y="170" width="160" height="40" rx="8" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="320" y="194" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Admin removes it, directly</text>
    <line x1="320" y1="56" x2="320" y2="90" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSdprop)" />
    <line x1="320" y1="142" x2="320" y2="170" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.2" markerEnd="url(#adSdprop)" />
    <path d="M240 190 L100 190 L100 33 L240 33" fill="none" stroke="#ef4444" strokeOpacity="0.6" strokeWidth="1.2" strokeDasharray="4 3" markerEnd="url(#adSdpropR)" />
    <text x="60" y="115" textAnchor="middle" fill="#ef4444" fontSize="8" opacity="0.85" transform="rotate(-90 60 115)">no lasting effect</text>
    <defs>
      <marker id="adSdprop" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
      <marker id="adSdpropR" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#ef4444" opacity="0.7" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>Cleaning the group's own ACL fixes nothing. The only fix is finding and removing the ACE on AdminSDHolder itself, which is also why so few incident responses actually catch it the first time.</p>
</div>

The same idea generalizes past AdminSDHolder: granting a low-privileged, easy-to-overlook account `WriteDACL` on an OU, `GenericAll` on a GPO, or, quietest of all, Replicating Directory Changes rights directly (skipping the need for any ticket forgery at all, straight to Part 3's DCSync) are all durable persistence, because none of them require a credential at all, only a standing permission nobody happened to be looking for.

**Requirements:** `WriteDACL` (or equivalent) on AdminSDHolder itself, which in turn usually requires having already reached Domain Admin or an equivalent high-privilege account at least once, exactly the moment this technique is meant to be planted in.

```bash
# grant a low-privileged account GenericAll on AdminSDHolder, PowerView-style
Add-DomainObjectAcl -TargetIdentity 'CN=AdminSDHolder,CN=System,DC=corp,DC=com' -PrincipalIdentity lowpriv-user -Rights All

# or with Impacket's dacledit.py, from Linux
dacledit.py -action write -rights FullControl -principal lowpriv-user -target-dn 'CN=AdminSDHolder,CN=System,DC=corp,DC=com' $DOMAIN/$USER:$PASSWORD
```

The ACE rides SDProp onto every protected group's members on the next cycle, without ever forging a ticket.

**Detection.** **Event 5136** (a directory service object was modified) on the AdminSDHolder object itself is rare enough in legitimate operation to be a very high-fidelity alert, and it's worth a permanent SACL specifically for that reason. More broadly, a scheduled BloodHound run against the domain's own ACLs, the same defensive habit recommended in Part 1 and Part 3, is what actually catches the general pattern: a low-privileged principal holding a dangerous ACE on a high-value object, whether or not that object happens to be AdminSDHolder specifically.

## Detection Recap

| Technique | Primary signal | Where to look |
|---|---|---|
| Silver Ticket | No corresponding KDC request for an accessed service | Absence of 4768/4769; ValidateKdcPacSignature enforcement |
| Diamond Ticket | PAC group membership inconsistent with AD | Correlate ticket contents against the directory, not the request itself |
| Sapphire Ticket | Transplanted PAC missing Requestor/Attributes structures | KDC_ERR_TGT_REVOKED on a DC patched per KB5008380; otherwise no reliable signal |
| Skeleton Key | Code injection / unsigned module in lsass.exe on a DC | LSASS Image File Execution Options auditing; authentication with a non-current password |
| DSRM backdoor | DsrmAdminLogonBehavior set to 1 or 2 | Registry auditing; event 4624 for the DSRM account name |
| Custom SSP | Unexpected entry in Security Packages; unsigned DLL in lsass.exe | Event 4657; Sysmon event 7 (ImageLoad) |
| AdminSDHolder ACL abuse | ACE modification on the AdminSDHolder object itself | Event 5136 with a SACL on AdminSDHolder; scheduled BloodHound ACL review |

## Conclusion

None of these techniques need to be re-discovered to be re-used. That's the actual defensive lesson: patching the vulnerability or misconfiguration that got an attacker in the first time does nothing to the Golden Ticket already forged, the SSP already loaded, or the ACE already sitting on AdminSDHolder. Persistence in Active Directory is rarely a backdoor in the traditional sense, it's a credential, a patched code path, or a permission, each one durable specifically because it doesn't look like an intrusion artifact at all. Detecting them means watching the small number of places they have to leave a trace, LSASS's own integrity, a handful of specific registry values, and the ACL on one object that most defenders have never looked at directly, rather than waiting for a second, louder compromise to notice the first one never actually ended.

## References & Further Reading

1. MITRE ATT&CK, "Steal or Forge Kerberos Tickets: Silver Ticket," T1558.002. [https://attack.mitre.org/techniques/T1558/002/](https://attack.mitre.org/techniques/T1558/002/)
	The classification behind the Silver Ticket section.
2. Semperis, "A Diamond Ticket in the Ruff." [https://www.semperis.com/blog/a-diamond-ticket-in-the-ruff/](https://www.semperis.com/blog/a-diamond-ticket-in-the-ruff/)
	The mechanics and detection guidance behind the Diamond Ticket section.
3. TrustedSec, "A Diamond in the Ruff." [https://trustedsec.com/blog/a-diamond-in-the-ruff](https://trustedsec.com/blog/a-diamond-in-the-ruff)
	Additional detail on why Diamond Tickets evade classic Golden Ticket detections.
4. MITRE ATT&CK, "Modify Authentication Process: Domain Controller Authentication," T1556.001. [https://attack.mitre.org/techniques/T1556/001/](https://attack.mitre.org/techniques/T1556/001/)
	The classification covering both the Skeleton Key and DSRM sections.
5. Sean Metcalf, "Active Directory Domain Controller Skeleton Key Malware & Mimikatz." [https://adsecurity.org/?p=1255](https://adsecurity.org/?p=1255)
	The original detailed writeup of the Skeleton Key attack and its detection.
6. Sean Metcalf, "Sneaky Active Directory Persistence #11: Directory Service Restore Mode (DSRM)." [https://adsecurity.org/?p=1714](https://adsecurity.org/?p=1714)
	The DsrmAdminLogonBehavior mechanics referenced in the DSRM section.
7. MITRE ATT&CK, "Boot or Logon Autostart Execution: Security Support Provider," T1547.005. [https://attack.mitre.org/techniques/T1547/005/](https://attack.mitre.org/techniques/T1547/005/)
	The classification behind the custom SSP section.
8. Sean Metcalf, "Sneaky Active Directory Persistence #12: Malicious Security Support Provider (SSP)." [https://adsecurity.org/?p=1760](https://adsecurity.org/?p=1760)
	The two custom SSP variants (registry-persistent and in-memory) referenced above.
9. MITRE ATT&CK, "Account Manipulation," T1098. [https://attack.mitre.org/techniques/T1098/](https://attack.mitre.org/techniques/T1098/)
	The classification behind the AdminSDHolder section.
10. GhostPack, Rubeus. [https://github.com/GhostPack/Rubeus](https://github.com/GhostPack/Rubeus)
	The tool behind the golden, silver, and diamond modules referenced throughout.
11. "Active Directory Series: Part 3 - From Enumeration to Privilege Escalation" (this blog). [/posts/active-directory-security-part3](/posts/active-directory-security-part3)
	The full Golden Ticket and DCSync mechanics this post builds on rather than repeating.
12. The Hacker Recipes, "Active Directory: Persistence." [https://www.thehacker.recipes/ad/persistence/](https://www.thehacker.recipes/ad/persistence/)
	A command-level reference for AD persistence techniques, used as a structural model for the requirements lines and command blocks added throughout this post.
13. The Hacker Recipes, "Sapphire Ticket." [https://www.thehacker.recipes/ad/movement/kerberos/forged-tickets/sapphire](https://www.thehacker.recipes/ad/movement/kerberos/forged-tickets/sapphire)
	The source for the Sapphire Ticket mechanics, the S4U2Self-plus-U2U PAC transplant, and the ticketer.py command referenced in this section.
14. Microsoft Security Response Center, KB5008380, "Authentication updates (CVE-2021-42287)." [https://support.microsoft.com/en-gb/topic/kb5008380-authentication-updates-cve-2021-42287-9dafac11-e0d0-4cb8-959a-143bd0201041](https://support.microsoft.com/en-gb/topic/kb5008380-authentication-updates-cve-2021-42287-9dafac11-e0d0-4cb8-959a-143bd0201041)
	The patch that added the Requestor and Attributes PAC structures a transplanted Sapphire Ticket PAC lacks, referenced in the Detection subsection.
15. Fortra, Impacket. [https://github.com/fortra/impacket](https://github.com/fortra/impacket)
	The source of ticketer.py, referenced in the Sapphire Ticket command block.
