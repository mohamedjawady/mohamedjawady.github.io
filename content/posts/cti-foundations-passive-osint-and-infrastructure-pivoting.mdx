---
title: "CTI Foundations: Part 3 - Passive OSINT and Infrastructure Pivoting"
description: "Part 3 of the CTI Foundations series. A practical pivoting methodology: starting from one indicator and walking outward through passive DNS, certificate transparency, and scan data to map an adversary's infrastructure footprint, without ever touching it directly."
date: "2026-06-10"
author: "Mohamed Habib Jaouadi"
tags: ["cti-foundations", "threat-intelligence", "osint", "infrastructure-pivoting", "threat-hunting", "passive-recon"]
banner: "/banners/posts/cti.avif"
bannerAlt: "Passive OSINT and infrastructure pivoting methodology for threat intelligence"
visibility: "public"
series: "CTI Foundations"
seriesOrder: 3
---

> **Pivoting Focus:** The Pyramid of Pain told you which layer of indicator is worth your time. This part is about the mechanics of actually walking from one indicator to the next, without ever sending a packet the adversary would notice.

**TL;DR:** Infrastructure pivoting starts from one artifact, an IP, a domain, a certificate, a hash, and walks outward through passive data sources to find everything else connected to it. Every pivot has to stay passive: querying an indexed dataset, never touching the adversary's live infrastructure directly. A single pivot hit is a lead, not a finding, shared hosting and free certificate authorities produce false corroboration constantly, which is why a real cluster needs agreement across more than one pivot direction, the same methodology that took Amnesty and Citizen Lab from roughly 200 seed domains to over 700 confirmed ones in the Pegasus investigation. The output is not a bigger indicator list, it is a map, and the map is what survives after every individual piece on it has rotated.

## Why This Stays Passive

Reconnaissance in an offensive engagement is allowed to be active: scan the target, probe its services, see what answers. CTI recon almost never has that option, and not just for legal reasons. The moment a query touches adversary-controlled infrastructure directly, resolving a domain against their own authoritative nameserver, connecting to their C2 port, submitting a sample to a sandbox that phones home, there is a real chance the adversary notices. A competent operator watching their own server logs sees a request from a security vendor's ASN and knows they've been found. Infrastructure that would have stayed up for another two weeks rotates that night, and the investigation loses the thread it was trying to pull.

<Term concept="infrastructure-pivoting">Passive pivoting</Term> avoids that entirely by querying datasets that already collected the answer somewhere else. <Term concept="passive-dns">Passive DNS</Term> providers log resolutions other clients already made. <Term concept="shodan">Shodan</Term> and <Term concept="censys">Censys</Term> scan the internet on their own schedule and let you query the results afterward. <Term concept="certificate-transparency">Certificate Transparency</Term> logs record every publicly trusted certificate at issuance, permanently, whether or not the analyst ever visits the site. None of these require the analyst to interact with the target at all. The adversary's logs stay exactly as boring as they were before the investigation started.

This is also why the pivot model in this post is not a shortcut. It's slower than active scanning by design. What it buys back is the ability to keep pulling the thread for as long as it stays useful, instead of burning it on the first query.

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 210" style={{ width: "100%", maxWidth: "620px" }} role="img" aria-label="Two rows contrasting active and passive reconnaissance. In the active row, an analyst directly queries adversary-controlled infrastructure, such as resolving a domain against its own nameserver or detonating a sample in a sandbox that phones home, and the adversary's own logs record the analyst's origin, often leading to infrastructure rotating that same night. In the passive row, the analyst queries an already-indexed dataset such as passive DNS or a certificate transparency log, and the adversary's infrastructure is never contacted at all, so nothing about the investigation reaches their logs.">
    <text x="15" y="20" fill="#ef4444" fontSize="10.5" fontWeight="bold" opacity="0.9">Active: touches adversary infrastructure directly</text>
    <rect x="10" y="32" width="130" height="44" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="75" y="58" textAnchor="middle" fill="currentColor" fontSize="10" fontWeight="bold">Analyst</text>
    <rect x="480" y="32" width="150" height="44" rx="7" fill="#ef4444" fillOpacity="0.1" stroke="#ef4444" strokeWidth="1.2" />
    <text x="555" y="54" textAnchor="middle" fill="currentColor" fontSize="10" fontWeight="bold">Adversary infra</text>
    <text x="555" y="67" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.7">logs every request</text>
    <line x1="140" y1="54" x2="480" y2="54" stroke="#ef4444" strokeOpacity="0.75" strokeWidth="1.3" markerEnd="url(#adOsintA)" />
    <text x="310" y="46" textAnchor="middle" fill="#ef4444" fontSize="8" opacity="0.9">direct DNS query, port scan, or sandbox detonation</text>
    <line x1="480" y1="72" x2="140" y2="72" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.1" markerEnd="url(#adOsintB)" />
    <text x="310" y="90" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.75">adversary sees the analyst's origin, rotates that night</text>

    <text x="15" y="130" fill="currentColor" fontSize="10.5" fontWeight="bold" opacity="0.85">Passive: queries a dataset that already collected the answer</text>
    <rect x="10" y="142" width="130" height="44" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="75" y="168" textAnchor="middle" fill="currentColor" fontSize="10" fontWeight="bold">Analyst</text>
    <rect x="480" y="142" width="150" height="44" rx="7" fill="#10b981" fillOpacity="0.12" stroke="#10b981" strokeWidth="1.2" />
    <text x="555" y="164" textAnchor="middle" fill="currentColor" fontSize="10" fontWeight="bold">Indexed dataset</text>
    <text x="555" y="177" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.7">passive DNS, CT logs, scan history</text>
    <line x1="140" y1="164" x2="480" y2="164" stroke="currentColor" strokeOpacity="0.55" strokeWidth="1.2" markerEnd="url(#adOsintC)" />
    <text x="310" y="156" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.75">query against data already collected elsewhere</text>
    <rect x="230" y="188" width="180" height="18" rx="4" fill="currentColor" fillOpacity="0.06" />
    <text x="320" y="201" textAnchor="middle" fill="#10b981" fontSize="8.5" fontWeight="bold">adversary's own logs never see this at all</text>
    <defs>
      <marker id="adOsintA" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#ef4444" opacity="0.85" />
      </marker>
      <marker id="adOsintB" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
      <marker id="adOsintC" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.55" />
      </marker>
    </defs>
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>Same question, two very different exposure profiles. Every tool in this post is chosen specifically because it answers from the bottom row.</p>
</div>

## The Pivot Model: One Artifact, Several Directions

Every pivot starts from a single seed, whatever indicator triggered the investigation, and asks the same question in a few different directions: what else shares this? An IP has hosted domains before and might host them again. A certificate gets reused across servers an operator controls, because generating a new one for every box is more work than it's worth. A sample communicates with infrastructure that other samples also communicate with. A registrant email shows up on other domains registered the same way.

None of these directions are exclusive. A mature pivot works several of them from the same seed and keeps whatever corroborates across more than one, since a single-source pivot is exactly the kind of low-confidence finding Part 1's confidence rubric warns about.

| Seed artifact | Pivots outward via | What it typically finds |
|---|---|---|
| IP address | Passive DNS, reverse DNS, ASN/netblock | Domains hosted there, neighboring infrastructure on the same provider |
| Domain | WHOIS history, passive DNS, subdomain enumeration | Registrant reuse across other domains, historical hosting moves |
| TLS certificate | Certificate Transparency logs, Shodan/Censys SAN search | Every other host presenting the same or a related certificate |
| JA3/JARM fingerprint | Shodan/Censys fingerprint search | Infrastructure running the same C2 framework, even on unrelated IPs |
| File hash | VirusTotal Relations, sandbox network telemetry | Other samples from the same builder, the C2 infrastructure they call out to |
| Registrant email | Reverse WHOIS | Every other domain registered with that address, a common operator mistake |

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 320" style={{ width: "100%", maxWidth: "560px" }} role="img" aria-label="A hub and spoke diagram with a single seed artifact at the center, branching out to five pivot directions: passive DNS resolution history, certificate transparency and SAN reuse, WHOIS registrant history, sample relations from sandboxes and VirusTotal, and scan fingerprint reuse across Shodan and Censys. Each spoke represents a different question asked of the same seed artifact, and a mature pivot works several of them at once rather than following only one.">
    <rect x="230" y="140" width="180" height="50" rx="9" fill="#ef4444" fillOpacity="0.14" stroke="#ef4444" strokeWidth="1.4" />
    <text x="320" y="162" textAnchor="middle" fill="currentColor" fontSize="11" fontWeight="bold">Seed Artifact</text>
    <text x="320" y="177" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.7">IP, domain, cert, hash...</text>

    <rect x="20" y="20" width="150" height="46" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="95" y="40" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Passive DNS</text>
    <text x="95" y="53" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.65">resolution history</text>

    <rect x="470" y="20" width="150" height="46" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="545" y="40" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Certificate reuse</text>
    <text x="545" y="53" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.65">CT logs, SAN search</text>

    <rect x="10" y="240" width="150" height="46" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="85" y="260" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">WHOIS history</text>
    <text x="85" y="273" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.65">registrant reuse</text>

    <rect x="480" y="240" width="150" height="46" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="555" y="260" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Sample relations</text>
    <text x="555" y="273" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.65">VirusTotal, sandbox</text>

    <rect x="245" y="270" width="150" height="46" rx="7" fill="#f59e0b" fillOpacity="0.12" stroke="#f59e0b" strokeWidth="1.2" />
    <text x="320" y="290" textAnchor="middle" fill="currentColor" fontSize="9.5" fontWeight="bold">Scan fingerprint</text>
    <text x="320" y="303" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.65">Shodan, Censys</text>

    <line x1="140" y1="66" x2="255" y2="140" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1.1" />
    <line x1="500" y1="66" x2="385" y2="140" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1.1" />
    <line x1="120" y1="240" x2="255" y2="190" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1.1" />
    <line x1="520" y1="240" x2="385" y2="190" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1.1" />
    <line x1="320" y1="270" x2="320" y2="190" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1.1" />
  </svg>
  <p style={{ fontSize: "0.85rem", opacity: 0.7, marginTop: "0.75rem" }}>None of these five directions are exclusive. Working two or three from the same seed, and keeping what corroborates across them, is what separates a pivot from a coincidence.</p>
</div>

## The Core Tools, by What They Actually Index

**<Term concept="passive-dns">Passive DNS</Term>** (SecurityTrails, DNSDB, RiskIQ) is the workhorse of domain-to-IP and IP-to-domain pivoting, covered in Part 2's infrastructure section. It answers "what has this IP resolved to, historically" and "what IPs has this domain pointed to, historically," which live DNS cannot, since live DNS only ever shows the current answer.

**<Term concept="shodan">Shodan</Term>** and **<Term concept="censys">Censys</Term>** both continuously scan the public internet and index what they find: open ports, banners, and, critically, the TLS certificate presented on each one, including every Subject Alternative Name in it. Searching for a certificate's serial number or fingerprint across either service surfaces every other host currently presenting that same certificate, a direct line to infrastructure an operator reused rather than regenerating for every box.

**<Term concept="certificate-transparency">Certificate Transparency</Term>**, queried through crt.sh or Censys's own certificate search, goes further than a live scan: it's a permanent, publicly auditable log of every certificate ever issued by a trusted CA, including ones for infrastructure that's already offline. Searching CT logs for a distinctive string in a SAN, an internal hostname pattern, a specific subdomain naming convention, finds infrastructure a live scan will never see again because it's already been torn down.

**<Term concept="virustotal">VirusTotal</Term>**'s Relations graph, already introduced in Part 2, pivots from a sample outward to the infrastructure it communicated with and from an IP or domain outward to every sample that called out to it, in both directions from the same query.

**<Term concept="urlscan">urlscan.io</Term>** submits a URL to a real, instrumented browser and records the entire resulting page load: every domain contacted, every redirect followed, a full screenshot. For phishing infrastructure specifically, this recovers the whole delivery chain from a single link, landing page, credential-harvesting form, exfil endpoint, in one pass.

**<Term concept="greynoise">GreyNoise</Term>** solves a different problem: the public internet is scanned constantly by security researchers, botnets, and background noise unrelated to any specific campaign. Before treating a scanning IP as adversary reconnaissance, checking GreyNoise confirms whether that IP scans everyone, in which case it's noise, not a lead.

**<Term concept="alienvault-otx">AlienVault OTX</Term>** is worth checking early rather than last: a pivot chain someone else already built and published as a "pulse" can save hours, and even a stale or partially wrong pulse is a useful cross-reference against what the analyst finds independently.

## A Worked Pivot Chain

The IPs and domains below use RFC 5737 documentation ranges and reserved names rather than real infrastructure, illustrative rather than a claim about any actual campaign.

**Seed:** a phishing report flags `login-secure-portal.example` as the credential-harvesting link.

**Pivot 1, urlscan.** Submitting the URL to urlscan shows the page loading a form that POSTs credentials to `203.0.113.44:8443`, and pulls in a favicon and a specific CSS bundle from the same host, both distinctive enough to fingerprint.

**Pivot 2, Shodan on the IP.** `203.0.113.44` is running a self-signed certificate with the CN `internal-auth.example` and a SAN list including two other hostnames never seen in the phishing report: `hr-portal-secure.example` and `payroll-access.example`. Same operator, same kit, redeployed under different lures.

**Pivot 3, Certificate Transparency.** Searching crt.sh for `internal-auth.example` turns up two more certificates issued for the same name over the preceding six weeks, each with a different IP in the SAN's resolution history, `203.0.113.51` and `203.0.113.67`, both in the same /24 as the original.

**Pivot 4, passive DNS on the /24.** Passive DNS resolution history for that block shows a consistent pattern: each IP hosts one phishing domain for roughly ten days before the domain moves to the next IP in sequence. That cadence, not any single indicator, is the actual finding, an operational pattern durable enough to write a hunt hypothesis around: watch newly observed certificates reusing the CN `internal-auth.example` regardless of which IP presents them next.

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", margin: "2.5rem 0" }}>
  <svg viewBox="0 0 640 150" style={{ width: "100%", maxWidth: "640px" }} role="img" aria-label="A five-step horizontal chain for the worked pivot example. Step one, the seed, a phishing link. Step two, urlscan reveals the credential-harvesting IP and port. Step three, Shodan on that IP reveals a self-signed certificate naming two more lure domains. Step four, Certificate Transparency logs reveal two more IPs presenting the same certificate name over the preceding weeks. Step five, passive DNS across that IP block reveals a ten-day rotation cadence, the actual durable finding.">
    <rect x="5" y="55" width="118" height="50" rx="7" fill="currentColor" fillOpacity="0.08" stroke="currentColor" strokeWidth="1" strokeOpacity="0.35" />
    <text x="64" y="76" textAnchor="middle" fill="currentColor" fontSize="8.5" fontWeight="bold">Seed</text>
    <text x="64" y="89" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.7">phishing link</text>
    <rect x="132" y="55" width="118" height="50" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="191" y="76" textAnchor="middle" fill="currentColor" fontSize="8.5" fontWeight="bold">urlscan</text>
    <text x="191" y="89" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.7">reveals IP:8443</text>
    <rect x="259" y="55" width="118" height="50" rx="7" fill="#0ea5e9" fillOpacity="0.12" stroke="#0ea5e9" strokeWidth="1.2" />
    <text x="318" y="76" textAnchor="middle" fill="currentColor" fontSize="8.5" fontWeight="bold">Shodan</text>
    <text x="318" y="89" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.7">cert CN + 2 more SANs</text>
    <rect x="386" y="55" width="118" height="50" rx="7" fill="#a855f7" fillOpacity="0.12" stroke="#a855f7" strokeWidth="1.2" />
    <text x="445" y="76" textAnchor="middle" fill="currentColor" fontSize="8.5" fontWeight="bold">crt.sh</text>
    <text x="445" y="89" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.7">2 more IPs, same CN</text>
    <rect x="513" y="55" width="122" height="50" rx="7" fill="#ef4444" fillOpacity="0.16" stroke="#ef4444" strokeWidth="1.4" />
    <text x="574" y="76" textAnchor="middle" fill="currentColor" fontSize="8.5" fontWeight="bold">Passive DNS</text>
    <text x="574" y="89" textAnchor="middle" fill="currentColor" fontSize="7.5" opacity="0.7">10-day rotation cadence</text>
    <line x1="123" y1="80" x2="132" y2="80" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.1" markerEnd="url(#adPivotChain)" />
    <line x1="250" y1="80" x2="259" y2="80" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.1" markerEnd="url(#adPivotChain)" />
    <line x1="377" y1="80" x2="386" y2="80" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.1" markerEnd="url(#adPivotChain)" />
    <line x1="504" y1="80" x2="513" y2="80" stroke="currentColor" strokeOpacity="0.5" strokeWidth="1.1" markerEnd="url(#adPivotChain)" />
    <text x="320" y="125" textAnchor="middle" fill="currentColor" fontSize="8" opacity="0.6">Every box left of the last one will be gone within weeks. The pattern in the last box might not be.</text>
    <defs>
      <marker id="adPivotChain" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor" opacity="0.5" />
      </marker>
    </defs>
  </svg>
</div>

Four pivots, starting from one link, end at a behavioral pattern that outlives every individual domain and IP involved. That's the point of the exercise: the IPs will all be gone in a month. The ten-day rotation cadence, and the certificate CN the operator kept reusing out of laziness, might not be.

## This Isn't a Toy Example

The illustrative chain above compresses into a page what real pivoting work usually takes days to build, but the methodology is exactly what's used in some of the most consequential infrastructure-mapping research publicly documented. Amnesty International's Security Lab and Citizen Lab's 2021 forensic work on NSO Group's Pegasus spyware is the clearest large-scale example: analysts started from a small, known set of roughly 201 infection domains observed in attacks against Moroccan journalists and human rights defenders, then pivoted outward through clustering patterns in domain registration, TLS certificate issuance, and network scanning, the same three pivot directions used in the worked example above, to identify over 700 related domains tied to the same infrastructure. Citizen Lab independently developed a substantially similar detection method and cross-validated Amnesty's findings against it, which is the pivot-log discipline from the next section operating at organizational scale: two separate teams, two separate pivot chains, converging on the same cluster.

The seed set was small. The methodology, not any single domain, is what scaled it to 700.

## When a Pivot Is Coincidence, Not Correlation

Not every shared artifact means shared ownership, and treating every pivot hit as a confirmed relationship is exactly the confirmation bias Part 1's Analyst Mindset section warns about: it's easy to see a match and stop looking for the reason it might not mean what it looks like it means.

Shared hosting is the most common false pivot. A single IP on a budget VPS provider or behind a CDN can serve thousands of unrelated domains belonging to unrelated customers; an IP-based pivot alone against that kind of host produces noise, not a cluster. The same applies to shared certificates issued by free, automated CAs like Let's Encrypt, where a matching issuer says nothing about the two certificate holders being related, only that both used the same free service everyone else does too. A registrant email pivot can just as easily surface a bulk domain reseller's own contact address, reused across thousands of customers' unrelated registrations, as it can surface an actual operator.

The corrective is the same one already built into the pivot model: never conclude a relationship from one pivot direction alone. A shared IP is a lead. A shared IP plus a shared, non-default certificate plus a matching rotation cadence is a finding. If a pivot only ever produces one kind of corroboration no matter how many hops deep it goes, that's usually a sign the artifact itself is too generic to pivot on, not that the cluster is real and just hard to prove.

## Organizing Findings So the Chain Survives You

A pivot chain that lives only in an analyst's browser history is worthless the moment that analyst is on leave. Every hop needs to be recorded with enough context that someone else can verify it or pick it up cold: what was queried, in which tool, on what date, what came back, and how confident the analyst is that this hop is actually related rather than coincidental.

<CollapsibleCode title="Pivot Log Template" language="markdown">

```markdown
PIVOT CHAIN: [short name / seed indicator]
Started: [date] | Analyst: [name] | Related requirement: [link to intelligence requirement]

HOP 1
- From: [seed artifact]
- Tool/source: [e.g. urlscan.io scan ID]
- Found: [new artifact]
- Confidence this hop is related: High/Medium/Low
- Why: [one line]

HOP 2
- From: [artifact from Hop 1]
- Tool/source: [...]
- Found: [...]
- Confidence: [...]
- Why: [...]

[repeat per hop]

SUMMARY
- Total artifacts found: [count]
- Pattern identified (if any): [e.g. rotation cadence, shared cert CN, shared registrant]
- Recommended hunt/detection: [what this pattern lets you watch for going forward]
- Confidence in overall cluster: High/Medium/Low (per Part 1's rubric)
```

</CollapsibleCode>

For anything beyond a single afternoon's pivot, a spreadsheet stops scaling and a graph becomes the right tool, because the relationships between artifacts (this cert was seen on that IP, that IP resolved from this domain) are the actual finding, not the artifacts in isolation. A <Term concept="tip">TIP</Term> like MISP models this natively: events hold the artifacts, objects group related ones together, and galaxy clusters tag the whole thing to a known actor or campaign once there's enough confidence to do so. The pivot log template above is the version that works before a finding is confident enough to formalize into MISP; promote it once it is.

## Where Passive Stops Being Free

Every tool named above has a free tier, and every free tier has a rate limit that a serious pivot chain will eventually hit. Shodan's free API allows a small number of query credits per month. VirusTotal's public API rate-limits aggressively and restricts some relationship data to paid tiers. SecurityTrails and DNSDB both gate historical passive DNS depth behind a subscription, and the historical depth is usually exactly what a real investigation needs, since the interesting pivot is rarely the current resolution.

This is precisely the gap a <Term concept="collection-management-framework">Collection Management Framework</Term> exists to make visible rather than discovered mid-investigation: which passive data sources does the program actually have paid access to, how far back does each one's history go, and which intelligence requirements simply can't be answered without a source the budget doesn't cover yet. A team that hits a rate limit mid-pivot and has no documented answer for "do we have a paid alternative" is finding that gap the expensive way, in the middle of a live investigation instead of during planning.

## Hands-On Exercise

Take any indicator you have legitimate access to investigate, an indicator from a past incident, a public threat report, or a sample from a malware repository. Starting from that single artifact, run at least three pivots across different tools (passive DNS, a certificate search, and one of VirusTotal Relations or urlscan). Document each hop using the template above, including your confidence and reasoning for each one.

At the end, write one sentence describing the durable pattern you found, not the list of IOCs, the behavioral or operational pattern connecting them. If you can't write that sentence, the pivot chain hasn't found a pattern yet, only a longer list of indicators, and Part 2's warning about pyramid inversion applies exactly as much to your own notes as it does to a vendor feed.

## Conclusion

Pivoting is not a tool skill, it's the practical application of everything Part 1 and Part 2 already established. The confidence rubric from Part 1 governs how much weight each hop deserves. The Pyramid of Pain from Part 2 tells you which artifacts, once found, are worth building lasting detection around versus which are only useful for the next few hours. This part was the missing piece connecting the two: how an analyst actually gets from one indicator to a mapped, documented cluster, without ever alerting the adversary that the map is being built.

## References & Further Reading

1. Shodan Search Engine. [https://www.shodan.io](https://www.shodan.io)
	Referenced throughout this post's pivot examples; free accounts provide enough query budget for initial investigations.
2. Censys Search. [https://censys.io](https://censys.io)
	The certificate-search-focused alternative to Shodan referenced in the Core Tools section.
3. crt.sh, Certificate Transparency Search. [https://crt.sh](https://crt.sh)
	The Certificate Transparency log search tool referenced in the worked pivot chain.
4. certificate.transparency.dev, "Certificate Transparency." [https://certificate.transparency.dev/](https://certificate.transparency.dev/)
	Background on how CT logs work and why they persist after infrastructure goes offline.
5. urlscan.io. [https://urlscan.io](https://urlscan.io)
	The instrumented-browser URL scanning service referenced in the Core Tools section and the worked pivot chain.
6. GreyNoise. [https://www.greynoise.io](https://www.greynoise.io)
	Referenced for filtering internet background noise out of scanning-IP investigations.
7. AlienVault Open Threat Exchange (OTX). [https://otx.alienvault.com](https://otx.alienvault.com)
	The community pulse-sharing platform referenced as an early cross-reference step.
8. VirusTotal. [https://www.virustotal.com](https://www.virustotal.com)
	The Relations-graph pivoting referenced from Part 2, extended here to the worked example.
9. MISP Threat Sharing Platform Documentation. [https://www.misp-project.org/documentation/](https://www.misp-project.org/documentation/)
	The events/objects/galaxy model referenced in the Organizing Findings section.
10. IETF RFC 5737, "IPv4 Address Blocks Reserved for Documentation." [https://datatracker.ietf.org/doc/html/rfc5737](https://datatracker.ietf.org/doc/html/rfc5737)
	The documentation-only address range (203.0.113.0/24) used throughout the worked pivot chain example.
11. Amnesty International Security Lab, "Forensic Methodology Report: How to Catch NSO Group's Pegasus" (2021). [https://securitylab.amnesty.org/latest/2021/07/forensic-methodology-report-how-to-catch-nso-groups-pegasus/](https://securitylab.amnesty.org/latest/2021/07/forensic-methodology-report-how-to-catch-nso-groups-pegasus/)
	The real-world pivoting methodology, from roughly 201 seed domains to over 700 clustered ones, referenced in the "This Isn't a Toy Example" section.
12. The Citizen Lab, "Independent Peer Review of Amnesty International's Forensic Methods for Identifying Pegasus Spyware" (2021). [https://citizenlab.ca/2021/07/amnesty-peer-review/](https://citizenlab.ca/2021/07/amnesty-peer-review/)
	The independent cross-validation referenced in the same section, two separately built pivot chains converging on the same infrastructure cluster.
13. "CTI Foundations: Part 1 - What Cyber Threat Intelligence Is and Why It Matters" (this blog). [/posts/cti-foundations-what-is-cti-and-why-it-matters](/posts/cti-foundations-what-is-cti-and-why-it-matters)
	The confidence rubric and analyst mindset section (mirror imaging, confirmation bias) this post's coincidence-vs-correlation guidance builds on.
14. "CTI Foundations: Part 2 - The Threat Intelligence Pyramid" (this blog). [/posts/cti-foundations-the-threat-intelligence-pyramid](/posts/cti-foundations-the-threat-intelligence-pyramid)
	The Infrastructure Pivoting Workflow subsection this post extends into a full methodology.
15. "Collection Management Framework" (this blog). [/posts/collection-management-framework](/posts/collection-management-framework)
	The framework referenced in the Where Passive Stops Being Free section for tracking which paid sources a program actually has access to.

## What's Next

In Part 4, we move from finding infrastructure to attributing it: using the Diamond Model to structure a cluster of pivoted artifacts into an actual adversary profile, and what confidence level that attribution actually deserves.
